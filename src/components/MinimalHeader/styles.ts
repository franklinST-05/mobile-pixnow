import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    header: {
        display: "flex",
        flexDirection: "row",
        alignItems: "center",
        paddingHorizontal: 20,
    },
    title: {
        fontSize: 16,
        fontWeight: "400",
        color: "#96f",
        marginLeft: 10,
    }
});